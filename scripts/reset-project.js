console.log("Resetting project..."); const fs = require("fs"); const path = require("path"); const appDir = path.join(__dirname, "../app"); if (fs.existsSync(appDir)) { fs.rmdirSync(appDir, { recursive: true }); } fs.mkdirSync(appDir); fs.writeFileSync(path.join(appDir, "index.tsx"), "import { Text, View } from \"react-native\";\n\nexport default function Index() {\n  return (\n    <View\n      style={{\n        flex: 1,\n        justifyContent: \"center\",\n        alignItems: \"center\",\n      }}\n    >\n      <Text>Edit app/index.tsx to edit this screen.</Text>\n    </View>\n  );\n}"); console.log("Project reset complete.");
